# Pudding mit Gabel - Environment Variables

# JWT Secret for authentication (REQUIRED)
# Generate a strong secret key for production using: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-here

# MongoDB Configuration (REQUIRED for admin features)
# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/puddingmeetup?retryWrites=true&w=majority

# MongoDB Database name (OPTIONAL)
MONGODB_DB=puddingmeetup

# Admin User Details (for reference)
# Admin email: notviqa@gmail.com
# Admin name: viqa
# Admin password: Set during admin user creation

# API URL for frontend (OPTIONAL - defaults to current domain)
NEXT_PUBLIC_API_URL=https://your-project-name.vercel.app

# Environment (automatically set by Vercel)
NODE_ENV=production
